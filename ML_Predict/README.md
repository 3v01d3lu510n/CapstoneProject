# Machine Learning Prediction Manual

> Notes: make sure the *idf_value.csv*, *random_forest_classifier.pkl* and *predict.py* in the same directory

Run the command:

```
py .\predict.py <file/directory_path>
```

After running, a prediction_log.txt file is generated.